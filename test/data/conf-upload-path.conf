http {
    server {
        listen 127.0.0.1:8080;
        root /tmp;
        upload_path /tmp/server-upload;

        location /upload {
            upload_path /tmp/location-upload;
        }
    }
}
